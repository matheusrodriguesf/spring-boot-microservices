server:
  port: 8888
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
         git:
           uri: https://github.com/matheusrodriguesf/spring-boot-config-server
           default-label: main
           search-paths: 'greeting-service*'
